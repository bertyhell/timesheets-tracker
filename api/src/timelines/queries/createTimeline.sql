INSERT INTO timelines
(
    id,
    title,
    timelineType,
    eventProviderInfo,
    createdAt,
    updatedAt,
    visualOrder
)
VALUES (:id, :title, :timelineType, :eventProviderInfo, :createdAt, :updatedAt, :visualOrder)
